This project demonstrates Infrastructure as Code by deploying a dummy agriculture web 
application using Docker and Kubernetes. The infrastructure is created and managed 
automatically using configuration files instead of manual setup.
